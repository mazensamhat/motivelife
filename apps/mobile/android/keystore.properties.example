# Copy to keystore.properties (never commit). Create keystore in Android Studio:
# Build → Generate Signed App Bundle → Create new...
storeFile=../keystore/motivelife-release.jks
storePassword=REPLACE_ME
keyAlias=motivelife
keyPassword=REPLACE_ME
