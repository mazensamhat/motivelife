"use client";

import { FamilyMapHeroVisual } from "@/components/marketing/family-marketing-visuals";

/** Dominant visual for MyMotiveFamily — live command center, not a tracker grid. */
export function FamilyCommandCenter() {
  return <FamilyMapHeroVisual />;
}
